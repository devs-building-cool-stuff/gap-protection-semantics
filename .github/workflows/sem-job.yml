name: sem-job

on:
  pull_request:
    paths:
      - "never/**"

jobs:
  gate:
    name: gapgate
    runs-on: ubuntu-latest
    steps:
      - name: ok
        run: echo "job-mediated check named gapgate"
