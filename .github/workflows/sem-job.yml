name: sem-job

on:
  pull_request: {}

jobs:
  gate:
    name: gapgate
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: ok
        run: echo "job-mediated check named gapgate"
